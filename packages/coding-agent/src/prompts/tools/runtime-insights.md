Observe a JavaScript, TypeScript, or Python program with instrumentation.
`code`/`path` selects the program; `insight`/`insightPath` selects the hooks.
Source loads and function enter/return events accompany program output.
One-shot runs emit no close event.
