Profile JavaScript or TypeScript{{#if python}}, or Python{{/if}}. `cputracing` records exact calls;
`cpusampling` collects lower-overhead statistical samples and SHOULD be used for
longer runs. Returns a text report.
