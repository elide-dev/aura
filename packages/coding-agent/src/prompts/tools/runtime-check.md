Resolve dependencies and compile supported source sets without producing
artifacts. This is a fast integrity probe, not project static analysis or a
TypeScript typecheck: TypeScript types are stripped. Run the project's declared
typecheck/check command when TypeScript correctness is the contract.
