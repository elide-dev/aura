Analyze JVM dependencies with `jdeps`. `path` accepts Java/Kotlin source, a
class, JAR, or class directory; alternatively provide `language` + `code`.
Sources compile in scratch space. `output` optionally writes a cwd-relative
report; replacing it requires `overwrite: true`.
